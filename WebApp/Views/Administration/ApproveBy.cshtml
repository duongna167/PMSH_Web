<div class="modal fade" id="appB_mainModal" tabindex="-1" aria-labelledby="appB_mainModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg-4by3 modal-dialog-centered modal-dialog-scrollable">
        <div class="modal-content">

            <div class="modal-header">
                <h5 class="modal-title" id="appB_mainModalLabel">Approved By</h5>
            </div>

            <div class="modal-body">
                <div class="search-filter">
                    <div>
                        <button id="appB_btnView" type="button" class="mui-button mui-button-search">View</button>
                        <button id="appB_btnNew" type="button" class="mui-button mui-button-new">New</button>
                        <button id="appB_btnEdit" type="button" class="mui-button mui-button-edit">Edit</button>
                        <button id="appB_btnDelete" type="button" class="mui-button mui-button-delete">Delete</button>
                        <button id="appB_btnExportExcel" type="button" class="mui-button mui-button-export">Excel</button>
                        <button type="button" class="mui-button mui-button-new" data-bs-dismiss="modal" aria-label="Close">Close</button>
                    </div>

                    <form id="appB_searchForm">
                        <div class="row g-2 mt-1">
                            <div class="col-12 col-md-4 col-lg-3">
                                <div class="row g-2 align-items-center">
                                    <label for="appB_codeSearch" class="col-md-4 col-form-label col-form-label-sm text-md-end">
                                        Code
                                    </label>
                                    <div class="col-md-8">
                                        <input id="appB_codeSearch"
                                               name="appB_codeSearch"
                                               type="text"
                                               class="form-control form-control-sm">
                                    </div>
                                </div>
                            </div>

                            <div class="col-12 col-md-4 col-lg-3">
                                <div class="row g-2 align-items-center">
                                    <label for="appB_nameSearch" class="col-md-4 col-form-label col-form-label-sm text-md-end">
                                        Name
                                    </label>
                                    <div class="col-md-8">
                                        <input id="appB_nameSearch"
                                               name="appB_nameSearch"
                                               type="text"
                                               class="form-control form-control-sm">
                                    </div>
                                </div>
                            </div>

                            <div class="col-12 col-md-4 col-lg-3">
                                <div class="row g-2 align-items-center">
                                    <label for="appB_showInactive" class="col-md-4 col-form-label col-form-label-sm text-md-end">
                                        Status
                                    </label>
                                    <div class="col-md-8">
                                        <div class="d-flex align-items-center h-100">
                                            <div class="form-check d-flex align-items-center gap-2 mb-0">
                                                <span class="status-color-box color-clean-uncheck"></span>
                                                <input id="appB_showInactive"
                                                       name="appB_showInactive"
                                                       type="checkbox">
                                                <label class="form-check-label mb-0" for="appB_showInactive">Show Inactive</label>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>

                <div class="mui-card">
                    <div id="appB_gridContainer" class="gridContainer"></div>
                </div>

                <div id="appB_loadingSpinner" class="text-center py-4" style="display:none;">
                    <div class="spinner-border text-primary" role="status">
                        <span class="visually-hidden">Loading...</span>
                    </div>
                </div>
            </div>

        </div>
    </div>
</div>

<div class="modal fade" id="appB_itemModal" tabindex="-1" aria-labelledby="appB_itemModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable">
        <div class="modal-content">

            <div class="modal-header">
                <h5 class="modal-title" id="appB_itemModalLabel">Approved By</h5>
            </div>

            <div class="modal-footer justify-content-start">
                <button type="button" id="appB_btnSave" class="mui-button mui-button-save">Save</button>
                <button type="button" data-bs-dismiss="modal" class="mui-button mui-button-close">Close</button>
            </div>

            <div class="modal-body">
                <form id="appB_form">
                    <div class="row g-2 mt-1 mb-2">
                        <div class="col-12 col-lg-6">
                            <div class="row g-2 align-items-center">
                                <label for="appB_code" class="col-md-2 col-lg-4 col-form-label col-form-label-sm text-md-end">
                                    Code
                                </label>
                                <div class="col-md-10 col-lg-8">
                                    <input id="appB_code"
                                            name="appB_code"
                                            type="text"
                                            class="form-control form-control-sm">
                                    <div class="invalid-feedback" id="appB_codeError"></div>
                                </div>
                            </div>
                        </div>

                        <div class="col-12 col-lg-6">
                            <div class="row g-2 align-items-center">
                                <label for="appB_inactive" class="col-md-2 col-lg-4 col-form-label col-form-label-sm text-md-end">
                                    Status
                                </label>
                                <div class="col-md-10 col-lg-8">
                                    <div class="form-check d-flex align-items-center gap-2 pt-1">
                                        <input id="appB_inactive"
                                                name="appB_inactive"
                                                type="checkbox">
                                        <label for="appB_inactive" class="form-check-label text-danger mb-0">Inactive</label>
                                    </div>
                                    <div class="invalid-feedback" id="appB_inactiveError"></div>
                                </div>
                            </div>
                        </div>

                        <div class="col-12">
                            <div class="row g-2 align-items-center">
                                <label for="appB_name" class="col-md-2 col-lg-2 col-form-label col-form-label-sm text-md-end">
                                    Name
                                </label>
                                <div class="col-md-10 col-lg-10">
                                    <input id="appB_name"
                                            name="appB_name"
                                            type="text"
                                            class="form-control form-control-sm">
                                    <div class="invalid-feedback" id="appB_nameError"></div>
                                </div>
                            </div>
                        </div>

                        <div class="col-12">
                            <div class="row g-2">
                                <label for="appB_description" class="col-md-2 col-lg-2 col-form-label col-form-label-sm text-md-end">
                                    Description
                                </label>
                                <div class="col-md-10 col-lg-10">
                                    <textarea id="appB_description"
                                                name="appB_description"
                                                class="form-control form-control-sm"
                                                rows="5"
                                                style="height:100px"></textarea>
                                    <div class="invalid-feedback" id="appB_descriptionError"></div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <input type="hidden" id="appB_id" name="appB_id" value="0" />
                </form>
            </div>
        </div>
    </div>
</div>

<script>

    var appB_Module = (function () {
        let selectedRowData = null;

        const selectors = {
            mainModal: "#appB_mainModal",
            itemModal: "#appB_itemModal",
            form: "#appB_form",
            searchForm: "#appB_searchForm",
            grid: "#appB_gridContainer",
            loading: "#appB_loadingSpinner",
            itemModalLabel: "#appB_itemModalLabel",

            btnView: "#appB_btnView",
            btnNew: "#appB_btnNew",
            btnEdit: "#appB_btnEdit",
            btnDelete: "#appB_btnDelete",
            btnSave: "#appB_btnSave",
            btnExportExcel: "#appB_btnExportExcel",

            codeSearch: "#appB_codeSearch",
            nameSearch: "#appB_nameSearch",
            showInactive: "#appB_showInactive",

            id: "#appB_id",
            code: "#appB_code",
            name: "#appB_name",
            description: "#appB_description",
            inactive: "#appB_inactive"
        };

        const endpoints = {
            getList: "/Administration/GetApprovedBy",
            save: "/Administration/ApprovedBySave",
            delete: "/Administration/DeleteApprovedBy/"
        };

        function init() {
            bindEvents();
            loadGrid();
        }

        function bindEvents() {
            $(selectors.btnView).off("click").on("click", loadGrid);
            $(selectors.btnNew).off("click").on("click", openAddModal);
            $(selectors.btnEdit).off("click").on("click", openEditModal);
            $(selectors.btnDelete).off("click").on("click", deleteRecord);
            $(selectors.btnSave).off("click").on("click", saveRecord);
            $(selectors.btnExportExcel).off("click").on("click", exportExcel);

            $(selectors.form).off("input change", "input, select, textarea").on("input change", "input, select, textarea", function () {
                clearFieldError(this);
            });

            $(selectors.itemModal).off("hidden.bs.modal").on("hidden.bs.modal", function () {
                resetForm();
                applyValidationErrors([], selectors.form);
            });
        }

        function openAddModal() {
            resetForm();
            $(selectors.id).val("0");
            $(selectors.itemModalLabel).text("Add Approved By");
            $(selectors.itemModal).modal("show");
        }

        function openEditModal() {
            if (!selectedRowData) {
                showToast("Warning", "Please select 1 row to edit.", false);
                return;
            }

            resetForm();
            fillForm(selectedRowData);
            $(selectors.itemModalLabel).text("Update Approved By");
            $(selectors.itemModal).modal("show");
        }

        function fillForm(data) {
            $(selectors.id).val(data.id || 0);
            $(selectors.code).val(data.code ?? "");
            $(selectors.name).val(data.name ?? "");
            $(selectors.description).val(data.description ?? "");
            $(selectors.inactive).prop("checked", !!data.inactive);
        }

        function resetForm() {
            $(selectors.form)[0].reset();
            $(selectors.id).val("0");
            applyValidationErrors([], selectors.form);
        }

        function loadGrid() {
            $(selectors.loading).show();

            $.ajax({
                url: endpoints.getList,
                type: "GET",
                cache: false,
                data: {
                    code: $(selectors.codeSearch).val(),
                    name: $(selectors.nameSearch).val(),
                    isActive: $(selectors.showInactive).prop("checked") ? 1 : 0
                },
                traditional: true,
                crossDomain: false,
                headers: {
                    "X-CSRF-TOKEN-HEADERNAME": $("input[name='__RequestVerificationToken']").val()
                },
                success: function (data) {
                    renderGrid(data || []);
                },
                error: function () {
                    showToast("Error", "Unable to load data", false);
                },
                complete: function () {
                    $(selectors.loading).hide();
                }
            });
        }

        function renderGrid(data) {
            $(selectors.grid).dxDataGrid({
                dataSource: data,
                keyExpr: "id",
                width: "100%",
                allowColumnResizing: true,
                columnResizingMode: "widget",
                selection: { mode: "single" },
                hoverStateEnabled: true,
                filterRow: { visible: true, applyFilter: "auto" },
                paging: { pageSize: 20 },
                pager: {
                    visible: true,
                    allowedPageSizes: [20, 50, "all"],
                    showPageSizeSelector: true,
                    showInfo: true,
                    showNavigationButtons: true
                },
                scrolling: { mode: "standard", showScrollbar: "always" },
                columns: [
                    { dataField: "id", caption: "ID", visible: false },
                    { dataField: "code", caption: "Code", width: 100},
                    { dataField: "name", caption: "Name", width: 150 },
                    { dataField: "description", caption: "Description", minWidth: 120, cssClass: "col-description"},
                    { dataField: "inactive", caption: "Inactive", width: 60,
                        cellTemplate: function (container, options) {
                            $("<div>")
                                .dxCheckBox({
                                    value: !!options.value,
                                    readOnly: true
                                })
                                .css("transform", "scale(0.8)")
                                .appendTo(container);
                        }
                     },
                    { dataField: "createdBy", caption: "Created By", width: 80 },
                    { dataField: "createdDate", caption: "Cre.Date", dataType: "date", format: "dd/MM/yyyy", width: 80 },
                    { dataField: "updatedBy", caption: "Updated By", width: 80 },
                    { dataField: "updatedDate", caption: "Upd.Date", dataType: "date", format: "dd/MM/yyyy", width: 80 }
                ],
                onSelectionChanged: function (e) {
                    selectedRowData = e.selectedRowsData.length > 0 ? e.selectedRowsData[0] : null;
                }
            });
        }

        function saveRecord() {
            const id = parseInt($(selectors.id).val()) || 0;
            const loginName = (localStorage.getItem("Loginname") || "").trim().replace(/^"(.*)"$/, "$1");
            const loginUser = parseInt(localStorage.getItem("userID")) || 0;

            const model = {
                ID: id,
                Code: $(selectors.code).val().trim(),
                Name: $(selectors.name).val().trim(),
                Description: $(selectors.description).val().trim(),
                Inactive: $(selectors.inactive).prop("checked"),
                CreatedBy: loginName,
                UpdatedBy: loginName,
                UserInsertID: loginUser,
                UserUpdateID: loginUser
            };

            $.ajax({
                url: endpoints.save,
                type: "POST",
                contentType: "application/json",
                cache: false,
                data: JSON.stringify(model),
                traditional: true,
                crossDomain: false,
                headers: {
                    "X-CSRF-TOKEN-HEADERNAME": $("input[name='__RequestVerificationToken']").val()
                },
                success: function (res) {
                    if (res.success) {
                        showToast("Success", res.message, true);
                        $(selectors.itemModal).modal("hide");
                        loadGrid();
                        return;
                    }

                    if (res.errors && res.errors.length > 0) {
                        applyValidationErrors(res.errors, selectors.form);

                        const generalError = res.errors.find(x => x.field === "general");
                        const toastMessage = generalError ? generalError.message : "Please check input fields.";

                        showToast("Warning", toastMessage, false);
                        return;
                    }

                    showToast("Error", res.message || "Save failed", false);
                },
                error: function () {
                    showToast("Error", "Request error!", false);
                }
            });
        }

        function deleteRecord() {
            if (!selectedRowData || selectedRowData.id === 0) {
                showToast("Warning", "Please select 1 row to delete.", false);
                return;
            }

            if (!confirm("Are you sure you want to delete this record? This action cannot be undone.")) {
                return;
            }

            $.ajax({
                url: endpoints.delete + selectedRowData.id,
                type: "POST",
                traditional: true,
                crossDomain: false,
                headers: {
                    "X-CSRF-TOKEN-HEADERNAME": $("input[name='__RequestVerificationToken']").val()
                },
                success: function (result) {
                    if (result.success) {
                        selectedRowData = null;
                        showToast("Success", "Delete Successfully!", true);
                        loadGrid();
                    } else {
                        showToast("Error", result.message || "Delete failed!", false);
                    }
                },
                error: function () {
                    showToast("Error", "Delete failed!", false);
                }
            });
        }

        function exportExcel() {
            const grid = $(selectors.grid).dxDataGrid("instance");
            if (!grid) {
                showToast("Warning", "Grid is not ready.", false);
                return;
            }

            const workbook = new ExcelJS.Workbook();
            const companyName = localStorage.getItem("CompanyName") || "Tên công ty chưa xác định";
            const worksheet = workbook.addWorksheet("Sheet 1");

            worksheet.addRow([companyName]);
            worksheet.mergeCells("A1:D1");
            worksheet.getRow(1).font = { bold: true };
            worksheet.getRow(1).alignment = { vertical: "middle", horizontal: "left" };

            const now = new Date();
            const exportDate = now.toLocaleString("vi-VN");
            worksheet.addRow(["", "", "", exportDate]);
            worksheet.getRow(2).alignment = { vertical: "middle", horizontal: "right" };

            worksheet.addRow([]);
            worksheet.addRow([]);

            const reportRow = worksheet.addRow(["ApprovedBy"]);
            worksheet.mergeCells("A5:D5");
            reportRow.font = { bold: true, size: 14 };
            reportRow.alignment = { vertical: "middle", horizontal: "center" };

            worksheet.addRow([]);

            DevExpress.excelExporter.exportDataGrid({
                component: grid,
                worksheet: worksheet,
                autoFilterEnabled: true,
                topLeftCell: { row: 7, column: 1 }
            }).then(function () {
                const lastRow = worksheet.lastRow.number + 2;
                worksheet.mergeCells(`A${lastRow}:D${lastRow}`);
                worksheet.getCell(`A${lastRow}`).alignment = { horizontal: "center" };
                worksheet.getCell(`A${lastRow}`).font = { italic: true };

                workbook.xlsx.writeBuffer().then(function (buffer) {
                    saveAs(
                        new Blob([buffer], { type: "application/octet-stream" }),
                        "ApprovedBy.xlsx"
                    );
                });
            });
        }

        function clearFieldError(element) {
            const $element = $(element);

            if ($element.hasClass("is-invalid")) {
                $element.removeClass("is-invalid");
            }

            const $feedback = $element.siblings(".invalid-feedback");
            if ($feedback.length) {
                $feedback.text("");
            }
        }

        return {
            init: init,
            loadGrid: loadGrid,
            openAddModal: openAddModal,
            openEditModal: openEditModal
        };
    })();

    window.openModalApprovedBy = function () {
        if (typeof appB_Module !== "undefined" && appB_Module.init) {
            appB_Module.init();
        }
    };
</script>