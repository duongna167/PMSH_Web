<div class="modal fade" id="vip_mainModal" tabindex="-1" aria-labelledby="vip_mainModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-4by3 modal-dialog-scrollable">
        <div class="modal-content">

            <div class="modal-header">
                <h5 class="modal-title" id="vip_mainModalLabel">VIP</h5>
            </div>

            <div class="modal-body p-2">
                <div class="search-filter">
                    <div>
                        <button id="vip_btnView" type="button" class="mui-button mui-button-search">View</button>
                        <button id="vip_btnNew" type="button" class="mui-button mui-button-new">New</button>
                        <button id="vip_btnEdit" type="button" class="mui-button mui-button-edit">Edit</button>
                        <button id="vip_btnDelete" type="button" class="mui-button mui-button-delete">Delete</button>
                        <button id="vip_btnExportExcel" type="button" class="mui-button mui-button-export">Excel</button>
                        <button type="button" class="mui-button mui-button-new" data-bs-dismiss="modal" aria-label="Close">Close</button>
                    </div>

                    <form id="vip_searchForm">
                        <div class="row g-2 mt-1">
                            <div class="col-12 col-md-4 col-lg-3">
                                <div class="row g-2 align-items-center">
                                    <label for="vip_codeSearch" class="col-md-4 col-form-label col-form-label-sm text-md-end">
                                        Code
                                    </label>
                                    <div class="col-md-8">
                                        <input id="vip_codeSearch"
                                               name="vip_codeSearch"
                                               type="text"
                                               class="form-control form-control-sm">
                                    </div>
                                </div>
                            </div>

                            <div class="col-12 col-md-4 col-lg-3">
                                <div class="row g-2 align-items-center">
                                    <label for="vip_nameSearch" class="col-md-4 col-form-label col-form-label-sm text-md-end">
                                        Name
                                    </label>
                                    <div class="col-md-8">
                                        <input id="vip_nameSearch"
                                               name="vip_nameSearch"
                                               type="text"
                                               class="form-control form-control-sm">
                                    </div>
                                </div>
                            </div>

                            <div class="col-12 col-md-4 col-lg-3">
                                <div class="row g-2 align-items-center">
                                    <label for="vip_showInactive" class="col-md-4 col-form-label col-form-label-sm text-md-end">
                                        Status
                                    </label>
                                    <div class="col-md-8">
                                        <div class="d-flex align-items-center h-100">
                                            <div class="form-check d-flex align-items-center gap-2">
                                                <span class="status-color-box color-clean-uncheck"></span>
                                                <input id="vip_showInactive"
                                                       name="vip_showInactive"
                                                       type="checkbox">
                                                <label class="form-check-label" for="vip_showInactive">Show Inactive</label>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>

                <div class="mui-card mt-1">
                    <div id="vip_gridContainer" class="gridContainer"></div>
                </div>

                <div id="vip_loadingSpinner"
                    style="position: absolute;top: 50%;left: 50%;transform: translate(-50%, -50%);
                    z-index: 999;background: rgba(255, 255, 255, 0.55); backdrop-filter: blur(2px);width: 100%;
                    height: 100%;display: flex;align-items: center;justify-content: center;">
                    <div class="spinner-border text-primary"></div>
                </div>
            </div>

        </div>
    </div>
</div>

<div class="modal fade" id="vip_itemModal" tabindex="-1" aria-labelledby="vip_itemModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable">
        <div class="modal-content">

            <div class="modal-header">
                <h5 class="modal-title" id="vip_itemModalLabel">VIP</h5>
            </div>

            <div class="search-filter">
                <button type="button" id="vip_btnSave" class="mui-button mui-button-save">Save</button>
                <button type="button" data-bs-dismiss="modal" class="mui-button mui-button-close">Close</button>
            </div>

            <div class="modal-body">
                <form id="vip_form">
                    <div class="row g-2 mt-1 mb-2">
                        <div class="col-12 col-lg-6">
                            <div class="row g-2 align-items-center">
                                <label for="vip_code" class="col-md-2 col-lg-4 col-form-label col-form-label-sm text-md-end">
                                    Code
                                </label>
                                <div class="col-md-10 col-lg-8">
                                    <input id="vip_code"
                                            name="vip_code"
                                            type="text"
                                            class="form-control form-control-sm">
                                    <div class="invalid-feedback" id="vip_codeError"></div>
                                </div>
                            </div>
                        </div>

                        <div class="col-12 col-lg-6">
                            <div class="row g-2 align-items-center">
                                <label for="vip_inactive" class="col-md-2 col-lg-4 col-form-label col-form-label-sm text-md-end">
                                    Status
                                </label>
                                <div class="col-md-10 col-lg-8">
                                    <div class="form-check d-flex align-items-center gap-2">
                                        <input id="vip_inactive"
                                                name="vip_inactive"
                                                type="checkbox">
                                        <label for="vip_inactive" class="form-check-label text-danger">Inactive</label>
                                    </div>
                                    <div class="invalid-feedback" id="vip_inactiveError"></div>
                                </div>
                            </div>
                        </div>

                        <div class="col-12">
                            <div class="row g-2 align-items-center">
                                <label for="vip_name" class="col-md-2 col-lg-2 col-form-label col-form-label-sm text-md-end">
                                    Name
                                </label>
                                <div class="col-md-10 col-lg-10">
                                    <input id="vip_name"
                                            name="vip_name"
                                            type="text"
                                            class="form-control form-control-sm">
                                    <div class="invalid-feedback" id="vip_nameError"></div>
                                </div>
                            </div>
                        </div>

                        <div class="col-12">
                            <div class="row g-2">
                                <label for="vip_description" class="col-md-2 col-lg-2 col-form-label col-form-label-sm text-md-end">
                                    Description
                                </label>
                                <div class="col-md-10 col-lg-10">
                                    <textarea id="vip_description"
                                                name="vip_description"
                                                class="form-control form-control-sm"
                                                rows="5"
                                                style="height:100px"></textarea>
                                    <div class="invalid-feedback" id="vip_descriptionError"></div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <input type="hidden" id="vip_id" name="vip_id" value="0" />
                </form>
            </div>
        </div>
    </div>
</div>

<script>
    var vip_Module = (function () {
        let selectedRowData = null;

        const selectors = {
            mainModal: "#vip_mainModal",
            itemModal: "#vip_itemModal",
            form: "#vip_form",
            searchForm: "#vip_searchForm",
            grid: "#vip_gridContainer",
            loading: "#vip_loadingSpinner",
            itemModalLabel: "#vip_itemModalLabel",

            btnView: "#vip_btnView",
            btnNew: "#vip_btnNew",
            btnEdit: "#vip_btnEdit",
            btnDelete: "#vip_btnDelete",
            btnSave: "#vip_btnSave",
            btnExportExcel: "#vip_btnExportExcel",

            codeSearch: "#vip_codeSearch",
            nameSearch: "#vip_nameSearch",
            showInactive: "#vip_showInactive",

            id: "#vip_id",
            code: "#vip_code",
            name: "#vip_name",
            description: "#vip_description",
            inactive: "#vip_inactive"
        };

        const endpoints = {
            getList: "/Administration/GetVIP",
            save: "/Administration/VIPSave",
            delete: "/Administration/DeleteVIP/"
        };

        function init() {
            bindEvents();
            loadGrid();
        }

        function bindEvents() {
            $(selectors.btnView).off("click").on("click", loadGrid);
            $(selectors.btnNew).off("click").on("click", openAddModal);
            $(selectors.btnEdit).off("click").on("click", openEditModal);
            $(selectors.btnDelete).off("click").on("click", deleteRecord);
            $(selectors.btnSave).off("click").on("click", saveRecord);
            $(selectors.btnExportExcel).off("click").on("click", exportExcel);

            $(selectors.form).off("input change", "input, select, textarea").on("input change", "input, select, textarea", function () {
                clearFieldError(this);
            });

            $(selectors.itemModal).off("hidden.bs.modal").on("hidden.bs.modal", function () {
                resetForm();
                applyValidationErrors([], selectors.form);
            });
        }

        function openAddModal() {
            resetForm();
            $(selectors.id).val("0");
            $(selectors.itemModalLabel).text("Add VIP");
            $(selectors.itemModal).modal("show");
        }

        function openEditModal() {
            if (!selectedRowData) {
                showToast("Warning", "Please select a row first!", false);
                return;
            }

            resetForm();
            fillForm(selectedRowData);
            $(selectors.itemModalLabel).text("Update VIP");
            $(selectors.itemModal).modal("show");
        }

        function fillForm(data) {
            $(selectors.id).val(data.id || 0);
            $(selectors.code).val(data.code ?? "");
            $(selectors.name).val(data.name ?? "");
            $(selectors.description).val(data.description ?? "");
            $(selectors.inactive).prop("checked", !!data.inactive);
        }

        function resetForm() {
            $(selectors.form)[0].reset();
            $(selectors.id).val("0");
            applyValidationErrors([], selectors.form);
        }

        function loadGrid() {
            $(selectors.loading).show();

            $.ajax({
                url: endpoints.getList,
                type: "GET",
                cache: false,
                data: {
                    code: $(selectors.codeSearch).val(),
                    name: $(selectors.nameSearch).val(),
                    inactive: $(selectors.showInactive).prop("checked") ? 1 : 0
                },
                traditional: true,
                crossDomain: false,
                headers: {
                    "X-CSRF-TOKEN-HEADERNAME": $("input[name='__RequestVerificationToken']").val()
                },
                success: function (data) {
                    renderGrid(data || []);
                },
                error: function () {
                    showToast("Error", "Unable to load data", false);
                },
                complete: function () {
                    $(selectors.loading).hide();
                }
            });
        }

        function renderGrid(data) {
            $(selectors.grid).dxDataGrid({
                dataSource: data,
                keyExpr: "id",
                height: 500,
                width: "100%",
                allowColumnResizing: true,
                columnResizingMode: "widget",
                selection: { mode: "single" },
                hoverStateEnabled: true,
                filterRow: { visible: true, applyFilter: "auto" },
                paging: { pageSize: 20 },
                pager: {
                    visible: true,
                    allowedPageSizes: [20, 50, "all"],
                    showPageSizeSelector: true,
                    showInfo: true,
                    showNavigationButtons: true
                },
                scrolling: { mode: "standard", showScrollbar: "always" },
                columns: [
                    { dataField: "id", caption: "ID", visible: false },
                    { dataField: "code", caption: "Code", width: "10%" },
                    { dataField: "name", caption: "Name", width: "20%" },
                    { dataField: "description", caption: "Description", width: "30%" },
                    { dataField: "inactive", caption: "Inactive", width: "6%" ,
                        cellTemplate: function (container, options) {
                            $("<div>")
                                .dxCheckBox({
                                    value: options.value == 1 || options.value === true,
                                    readOnly: true
                                })
                                .css("transform", "scale(0.8)")
                                .appendTo(container);
                        }
                    },
                    { dataField: "createdBy", caption: "Created By", width: "8%" },
                    { dataField: "createdDate", caption: "Created Date", dataType: "date", width: "9%" },
                    { dataField: "updatedBy", caption: "Updated By", width: "8%" },
                    { dataField: "updatedDate", caption: "Updated Date", dataType: "date", width: "9%" }
                ],
                onSelectionChanged: function (e) {
                    selectedRowData = e.selectedRowsData.length > 0 ? e.selectedRowsData[0] : null;
                }
            });
        }

        function saveRecord() {
            const id = parseInt($(selectors.id).val()) || 0;
            const loginName = (localStorage.getItem("Loginname") || "").trim().replace(/^"(.*)"$/, "$1");
            const loginUser = parseInt(localStorage.getItem("userID")) || 0;

            const model = {
                ID: id,
                Code: $(selectors.code).val().trim(),
                Name: $(selectors.name).val().trim(),
                Description: $(selectors.description).val().trim(),
                Inactive: $(selectors.inactive).prop("checked"),
                CreatedBy: loginName,
                UpdatedBy: loginName,
                UserInsertID: loginUser,
                UserUpdateID: loginUser
            };

            $.ajax({
                url: endpoints.save,
                type: "POST",
                contentType: "application/json",
                cache: false,
                data: JSON.stringify(model),
                traditional: true,
                crossDomain: false,
                headers: {
                    "X-CSRF-TOKEN-HEADERNAME": $("input[name='__RequestVerificationToken']").val()
                },
                success: function (res) {
                    if (res.success) {
                        showToast("Success", res.message, true);
                        $(selectors.itemModal).modal("hide");
                        loadGrid();
                        return;
                    }

                    if (res.errors && res.errors.length > 0) {
                        applyValidationErrors(res.errors, selectors.form);

                        const generalError = res.errors.find(x => x.field === "general");
                        const toastMessage = generalError ? generalError.message : "Please check input fields.";

                        showToast("Warning", toastMessage, false);
                        return;
                    }

                    showToast("Error", res.message || "Save failed", false);
                },
                error: function () {
                    showToast("Error", "Request error!", false);
                }
            });
        }

        function deleteRecord() {
            if (!selectedRowData || selectedRowData.id === 0) {
                showToast("Warning", "Select 1 row, please!", false);
                return;
            }

            if (!confirm("Are you sure you want to delete this record? This action cannot be undone.")) {
                return;
            }

            $.ajax({
                url: endpoints.delete + selectedRowData.id,
                type: "POST",
                traditional: true,
                crossDomain: false,
                headers: {
                    "X-CSRF-TOKEN-HEADERNAME": $("input[name='__RequestVerificationToken']").val()
                },
                success: function (result) {
                    if (result.success) {
                        selectedRowData = null;
                        showToast("Success", "Delete Successfully!", true);
                        loadGrid();
                    } else {
                        showToast("Error", result.message || "Delete failed!", false);
                    }
                },
                error: function () {
                    showToast("Error", "Delete failed!", false);
                }
            });
        }

        function exportExcel() {
            const grid = $(selectors.grid).dxDataGrid("instance");
            if (!grid) {
                showToast("Warning", "Grid is not ready.", false);
                return;
            }

            const workbook = new ExcelJS.Workbook();
            const companyName = localStorage.getItem("CompanyName") || "Tên công ty chưa xác định";
            const worksheet = workbook.addWorksheet("Sheet 1");

            worksheet.addRow([companyName]);
            worksheet.mergeCells("A1:D1");
            worksheet.getRow(1).font = { bold: true };
            worksheet.getRow(1).alignment = { vertical: "middle", horizontal: "left" };

            const now = new Date();
            const exportDate = now.toLocaleString("vi-VN");
            worksheet.addRow(["", "", "", exportDate]);
            worksheet.getRow(2).alignment = { vertical: "middle", horizontal: "right" };

            worksheet.addRow([]);
            worksheet.addRow([]);

            const reportRow = worksheet.addRow(["Vip"]);
            worksheet.mergeCells("A5:D5");
            reportRow.font = { bold: true, size: 14 };
            reportRow.alignment = { vertical: "middle", horizontal: "center" };

            worksheet.addRow([]);

            DevExpress.excelExporter.exportDataGrid({
                component: grid,
                worksheet: worksheet,
                autoFilterEnabled: true,
                topLeftCell: { row: 7, column: 1 }
            }).then(function () {
                const lastRow = worksheet.lastRow.number + 2;
                worksheet.mergeCells(`A${lastRow}:D${lastRow}`);
                worksheet.getCell(`A${lastRow}`).alignment = { horizontal: "center" };
                worksheet.getCell(`A${lastRow}`).font = { italic: true };

                workbook.xlsx.writeBuffer().then(function (buffer) {
                    saveAs(
                        new Blob([buffer], { type: "application/octet-stream" }),
                        "VIP.xlsx"
                    );
                });
            });
        }

        function clearFieldError(element) {
            const $element = $(element);

            if ($element.hasClass("is-invalid")) {
                $element.removeClass("is-invalid");
            }

            const $feedback = $element.siblings(".invalid-feedback");
            if ($feedback.length) {
                $feedback.text("");
            }
        }

        return {
            init: init,
            loadGrid: loadGrid,
            openAddModal: openAddModal,
            openEditModal: openEditModal
        };
    })();

    $(document).ready(function () {
        vip_Module.init();
    });

    window.openModalVIP = function () {
        $("#vip_mainModal").modal("show");
        vip_Module.loadGrid();
    };
</script>