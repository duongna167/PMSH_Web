@using DevExpress.XtraReports.UI

@{
    var selectedReport = ViewBag.ReportHeader;
    var currencylist = ViewBag.CurrencyList;
}

<div class="modal fade" id="ar_opening_modal" data-bs-backdrop="static" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-xl">
        <div class="modal-content modal-full-height">
            <div class="modal-header bg-light py-2">
                <h5 class="modal-title">AR - Balance brought forward</h5>
            </div>

            <div class="modal-body d-flex flex-column" style="height: calc(100vh - 120px);">
                <div class="py-2">
                    <button id="ar_opening_btnPrint" class="mui-button mui-button-search" style="display: none;">View</button>
                    <button id="ar_opening_btnOkAuto" class="mui-button mui-button-new">Ok</button>
                    <button id="ar_opening_btnExportExcel" class="mui-button mui-button-export" type="button">Excel</button>
                    <button class="mui-button mui-button-close" data-bs-dismiss="modal">Close</button>
                </div>

                <div class="mui-card mb-2 bg-light border-0 d-flex flex-column flex-grow-1 overflow-hidden">
                    <div class="gridContainer mt-2 d-flex flex-column flex-grow-1" style="min-height: 0;">
                        <div id="ar_opening_gridContainer"></div>
                        <div id="ar_opening_loadingSpinner" style="display:none; position:absolute; top:50%; left:50%; transform:translate(-50%, -50%); z-index:1001;">
                            <i class="fa-solid fa-spinner fa-spin fa-2x text-primary"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    var ar_opening_Module = {
        selectedRowData: null,
        currencyList: @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(ViewBag.CurrencyList ?? new List<object>())),

        init: function () {
            this.initEvents();
            this.initDOMEvents();
        },

        initEvents: function () {
            window.openModalAROpening = () => {
                $('#ar_opening_modal').modal('show');
                this.reloadData();
            };
        },

        initDOMEvents: function () {
            $("#ar_opening_btnPrint").on("click", () => this.reloadData());
            $("#ar_opening_btnOkAuto").on("click", () => this.saveData());
            $("#ar_opening_btnExportExcel").on("click", () => this.exportExcel());

            // Reset khi đóng modal
            $('#ar_opening_modal').on('hidden.bs.modal', () => {
                this.selectedRowData = null;
            });
        },

        sortObjectKeysAlphabetically: function (dataArray) {
            return dataArray.map(obj => {
                const newObj = {};
                Object.keys(obj)
                    .sort((a, b) => a.localeCompare(b))
                    .forEach(key => {
                        const newKey = key.charAt(0).toLowerCase() + key.slice(1);
                        newObj[newKey] = obj[key];
                    });
                return newObj;
            });
        },

        reloadData: function () {
            $("#ar_opening_loadingSpinner").show();
            $.ajax({
                url: '/Accounting/AROpeningData',
                type: 'GET',
                headers: { "X-CSRF-TOKEN-HEADERNAME": $("input[name='__RequestVerificationToken']").val() },
                success: (result) => {
                    if (result) {
                        const sortedKeysData = this.sortObjectKeysAlphabetically(result);
                        const sortedByAccountName = sortedKeysData.sort((a, b) =>
                            (a.accountName || "").localeCompare(b.accountName || "")
                        );
                        this.initializeGrid(sortedByAccountName);
                    }
                    $("#ar_opening_loadingSpinner").hide();
                },
                error: () => {
                    showToast('Error', "Không thể tải báo cáo.", false);
                    $("#ar_opening_loadingSpinner").hide();
                }
            });
        },

        initializeGrid: function (data) {
            const self = this;
            $("#ar_opening_gridContainer").dxDataGrid({
                dataSource: data,
                height: "100%",
                width: "100%",
                showBorders: true,
                hoverStateEnabled: true,
                wordWrapEnabled: true,
                allowColumnResizing: true,
                columnResizingMode: "widget",
                columnAutoWidth: true,
                selection: { mode: "single" },
                scrolling: {
                    mode: "standard",
                    showScrollbar: "always",
                    useNative: true
                },
                paging: { pageSize: 25 },
                pager: {
                    visible: true,
                    allowedPageSizes: [25, 50, 100],
                    showPageSizeSelector: true,
                    showInfo: true,
                    showNavigationButtons: true
                },
                filterRow: { visible: true, applyFilter: "auto" },
                columns: [
                    { dataField: "id", visible: false },
                    { dataField: "accountName", caption: "Account Name", width: "18%", allowEditing: false },
                    { dataField: "city", caption: "City", width: "8%", allowEditing: false },
                    { dataField: "balance", caption: "Balance", width: "10%", format: "#,##0.##", allowEditing: false },
                    {
                        dataField: "currencyID",
                        caption: "Currency",
                        width: "8%",
                        allowEditing: true,
                        lookup: {
                            dataSource: this.currencyList,
                            valueExpr: "ID",
                            displayExpr: "ID"
                        },
                        setCellValue: function (rowData, value) {
                            rowData.currencyID = value || null;
                            if (self.selectedRowData && self.selectedRowData.id === rowData.id) {
                                self.selectedRowData.currencyID = value || null;
                            }
                        }
                    },
                    { dataField: "contactName", caption: "Contact Name", width: "15%", allowEditing: false },
                    { dataField: "type", caption: "Type", width: "7%", allowEditing: false },
                    { dataField: "accountNo", caption: "Account No", width: "10%", allowEditing: false },
                    { dataField: "createdBy", caption: "Created By", width: "8%", allowEditing: false },
                    { dataField: "createdDate", caption: "Created Date", width: "10%", dataType: "date", format: "dd/MM/yyyy", allowEditing: false },
                    { dataField: "updatedBy", caption: "Updated By", width: "8%", allowEditing: false },
                    { dataField: "updatedDate", caption: "Updated Date", width: "10%", dataType: "date", format: "dd/MM/yyyy", allowEditing: false },
                    { dataField: "arid", caption: "ARID", width: "5%", allowEditing: false }
                ],
                onRowClick: (e) => {
                    this.selectedRowData = e.data;
                }
            });
        },

        saveData: function () {
            if (!this.selectedRowData) {
                showToast("Warning", "Please select 1 row.", false);
                return;
            }

            $("#ar_opening_loadingSpinner").show();
            const dataPost = {
                ...this.selectedRowData,
                user: localStorage.getItem("Loginname")
            };

            $.ajax({
                url: '/Accounting/AROpeningSave',
                type: 'POST',
                data: dataPost,
                headers: { "X-CSRF-TOKEN-HEADERNAME": $("input[name='__RequestVerificationToken']").val() },
                success: (result) => {
                    if (result.success) {
                        showToast('Success', result.message, true);
                        this.reloadData();
                    } else {
                        showToast('Error', result.message || 'Save failed.', false);
                    }
                    $("#ar_opening_loadingSpinner").hide();
                },
                error: () => {
                    showToast('Error', "Lỗi hệ thống khi lưu.", false);
                    $("#ar_opening_loadingSpinner").hide();
                }
            });
        },

        exportExcel: function () {
            const grid = $("#ar_opening_gridContainer").dxDataGrid("instance");
            if (!grid) {
                showToast("Warning", "No data to export!", false);
                return;
            }
            const workbook = new ExcelJS.Workbook();
            const companyName = localStorage.getItem("CompanyName") || "Tên công ty chưa xác định";
            const worksheet = workbook.addWorksheet("Sheet 1");

            // === Dòng 1: Tên công ty (merge theo 4 cột) ===
            worksheet.addRow([companyName]);
            worksheet.mergeCells('A1:D1');
            worksheet.getRow(1).font = { bold: true };
            worksheet.getRow(1).alignment = { vertical: 'middle', horizontal: 'left' };

            // === Dòng 2: Ngày giờ xuất (Sửa lỗi hiển thị ở đây) ===
            const now = new Date();
            const exportDateStr = "Export Date: " + now.toLocaleString("vi-VN");
            const row2 = worksheet.getRow(2);
            row2.getCell(1).value = exportDateStr; // Gán vào ô đầu tiên của vùng merge (A2)
            worksheet.mergeCells('A2:D2');
            row2.alignment = { vertical: 'middle', horizontal: 'left' };
            row2.font = { italic: true };

            // === Dòng 3–4: trống
            worksheet.addRow([]);
            worksheet.addRow([]);

            // === Dòng 5: Tiêu đề báo cáo (ở giữa) ===
            const reportRow = worksheet.addRow(['AR - Balance brought forward']);
            worksheet.mergeCells('A5:D5');
            reportRow.font = { bold: true, size: 14 };
            reportRow.alignment = { vertical: 'middle', horizontal: 'center' };

            // === Dòng 6: trống
            worksheet.addRow([]);

            // === Xuất dữ liệu grid bắt đầu từ dòng 7 ===
            DevExpress.excelExporter.exportDataGrid({
                component: grid,
                worksheet: worksheet,
                autoFilterEnabled: true,
                topLeftCell: { row: 7, column: 1 }
            }).then(function () {
                // Footer (sau cùng)
                const lastRow = worksheet.lastRow.number + 2;
                worksheet.mergeCells(`A${lastRow}:D${lastRow}`);
                worksheet.getCell(`A${lastRow}`).alignment = { horizontal: 'center' };
                worksheet.getCell(`A${lastRow}`).font = { italic: true };

                // Ghi file
                let fileName = "AROpeningBalance.xlsx";
                workbook.xlsx.writeBuffer().then(function (buffer) {
                    saveAs(new Blob([buffer], { type: "application/octet-stream" }), fileName);
                });
            });
        },

    };

    $(document).ready(function () {
        ar_opening_Module.init();
        // Tự động View khi sẵn sàng (nếu modal mở sẵn)
        if ($('#ar_opening_modal').hasClass('show')) {
            ar_opening_Module.reloadData();
        }
    });
</script>
